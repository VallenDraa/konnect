// import { createContext } from 'react';
// import useUrlHistory from '../../utils/useUrlHistory/useUrlHistory';

// export const urlHistoryContext = createContext({ prev: null, current: null });

// export default function urlHistoryContextProvider({ children }) {
//   const [urlHistory, urlHistoryError] = useUrlHistory();

//   return (
//     <urlHistoryContext.Provider value={{ urlHistory, urlHistoryError }}>
//       {children}
//     </urlHistoryContext.Provider>
//   );
// }
